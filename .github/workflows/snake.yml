name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: generate snake svg/gif
        uses: Platane/snk@v3
        with:
          github_user_name: Chirag-Matta
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
            dist/github-snake.gif?color_snake=#a855f7&color_dots=#0d1b2a,#1b4965,#2a9d8f,#4ecdc4,#7ee8fa&color_background=#0d1117

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: generate full multi-year heatmap
        env:
          CONTRIB_TOKEN: ${{ secrets.CONTRIB_TOKEN }}
          GH_LOGIN: Chirag-Matta
        run: node scripts/generate-full-heatmap.mjs

      - name: push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
